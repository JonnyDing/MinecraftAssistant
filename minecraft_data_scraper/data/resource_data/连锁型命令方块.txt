功能与普通的命令方块相同。但是它只在它背后的命令方块方块执行命令时，并且处于激活状态，才会一起执行命令。
在你放置它的物品形式时，连锁命令方块的“保持开启”状态会默认打开。
它的ID与一般的命令方块不同（chain_command_block），这意味这你可以直接用/give获得它。当然，你也可以在命令方块的GUI界面中切换运行模式为“连锁”来转化。
连锁型命令方块在Java版1.9快照15w34a中加入。